// tPacket_NMatchup_NChat_NN_WaitRoom
module.exports = (socket, sequence) => {
    // Implement the packet logic here
    console.log('Sending tPacket_NMatchup_NChat_NN_WaitRoom');
    // Example: 
    // const packet = createPacket(sequence, PACKET_TYPES.tPacket_NMatchup_NChat_NN_WaitRoom);
    // send_packet(socket, packet);
};

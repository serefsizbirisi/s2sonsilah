// tPacket_NMatchup_NRoom_SN_ChangeName
module.exports = (socket, sequence) => {
    // Implement the packet logic here
    console.log('Sending tPacket_NMatchup_NRoom_SN_ChangeName');
    // Example: 
    // const packet = createPacket(sequence, PACKET_TYPES.tPacket_NMatchup_NRoom_SN_ChangeName);
    // send_packet(socket, packet);
};

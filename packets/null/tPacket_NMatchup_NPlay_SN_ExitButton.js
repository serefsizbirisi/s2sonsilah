// tPacket_NMatchup_NPlay_SN_ExitButton
module.exports = (socket, sequence) => {
    // Implement the packet logic here
    console.log('Sending tPacket_NMatchup_NPlay_SN_ExitButton');
    // Example: 
    // const packet = createPacket(sequence, PACKET_TYPES.tPacket_NMatchup_NPlay_SN_ExitButton);
    // send_packet(socket, packet);
};
